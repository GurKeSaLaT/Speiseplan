# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtualenv
.venv/
venv/

# SQLite runtime files (die eigentliche speiseplan.db bleibt versioniert)
instance/*.db-journal
instance/*.db-wal
instance/*.db-shm

# Automatisch generierter Flask-Signierschlüssel (CSRF/Sessions, siehe
# app.py: load_or_create_secret_key) - pro Umgebung eigenständig erzeugt,
# gehört NICHT ins Repo (sonst könnten alle Deployments dieselben Tokens
# fälschen).
instance/secret_key

# Editor/OS
.DS_Store
